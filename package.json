{
  "name": "taskmanager19",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "start": "turbo run start",
    "client-dev": "turbo run dev --filter client",
    "server-dev": "turbo run dev --filter server",
    "server-start": "turbo run start --filter server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "turbo": "^2.4.0"
  }
}
